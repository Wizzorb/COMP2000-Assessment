import javax.swing.*;

//This is the Controller in the MVC pattern
public class automatedCheckoutSystem {
    Double id = Double.parseDouble("0");
    Double name = Double.parseDouble("Test");
    Double price = Double.parseDouble("420.69");
    public stockDatabase testStock = new stockDatabase();

    public void testDB() {
        //testStock.updateStock(id, name, price);
    }
}
